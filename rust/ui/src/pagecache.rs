////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// Template Based Cache
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

use crate::rescache::CacheEntry;

pub struct Page {
    pub id: String,
    pub name: String,
    pub icon: String,
    pub help: String,
}

pub type PageCacheEntry = CacheEntry<Page>;
